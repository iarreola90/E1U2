{
  "version": "0.2.0",
  "configurations": [
    {
      "type": "chrome",
      "request": "launch",
      "name": "Start app with Chrome",
      "url": "http://localhost:4200",
      "preLaunchTask": "npm: start",
      "webRoot": "${workspaceFolder}"
    },
    {
      "type": "chrome",
      "request": "launch",
      "name": "Start app headless",
      "url": "http://localhost:4200",
      "runtimeArgs": ["--headless"],
      "preLaunchTask": "npm: start",
      "webRoot": "${workspaceFolder}"
    }
  ]
}
